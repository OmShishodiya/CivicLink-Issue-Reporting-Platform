{
  "name": "civiclink",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm install rollup --no-save && npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}

